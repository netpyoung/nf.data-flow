/*
 * generated {{date}}
 */
namespace AutoGenerated.DB
{
    using System;
    using SQLite.Attribute;

    {% for item in list %}
    [Export]
    public class {{item.name}}
    {
        {% for member in item.class_members %}
        {% if member.is_server_only %}
        // defined on server
        {% endif %}{% for attribute in member.attributes %}{% if member.is_server_only %}// {% endif %}[{{attribute}}]
        {% endfor %}{% if member.is_server_only %}// {% endif %}public {{member.type}} {{member.name}} { get; set; }{% endfor %}
    }
    {% endfor %}
}
