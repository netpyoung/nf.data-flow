/*
 * generated {{date}}
 */
using System;

namespace AutoGenerated.DB
{
       	{% for item in list %}{% if item.is_client %}
       	{% for attr in item.attributes %}[{{attr}}]
       	{% endfor %}public enum {{item.name}}
       	{
       		{% for arg in item.args %}{% for attr in arg.attributes %}[{{attr}}]
       		{% endfor %}{{arg.name}}{% if arg.value %} = {{arg.value}}{% endif %},{% if arg.desc %}// {{arg.desc}}{% endif %}
       		{% endfor %}
       	}{% endif %}{% endfor %}
}