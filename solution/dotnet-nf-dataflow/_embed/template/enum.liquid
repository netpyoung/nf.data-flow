using System;
using System.ComponentModel;

namespace {{ data.sheet_info.sheet_namespace }}
{
    public enum {{ data.sheet_info.sheet_name }}
    {
        {%- for content in data.contents -%}
        {{ content.attr }}
        [Description("{{ content.desc }}")]
        {{ content.name }} = {{ content.value }},
        {%- endfor -%}
    }
}