using System;
using System.ComponentModel;

namespace {{ data.SheetInfo.SheetNamespace }}
{
    public enum {{ data.SheetInfo.SheetName }}
    {
        {%- for content in data.Contents -%}
        {{ content.Attr }}
        [Description("{{ content.Desc }}")]
        {{ content.Name }} = {{ content.Value }},
        {%- endfor -%}
    }
}